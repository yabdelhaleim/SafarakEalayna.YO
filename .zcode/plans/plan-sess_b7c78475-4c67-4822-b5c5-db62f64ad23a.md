User wants the original /bus/treasury page restored AND the new P&L widget to coexist on the same page. The bug is that the current widget block pushes the cashbox/bank/wallet cards out of view, leaving the page looking like a single P&L widget floating alone.

## Root cause

In commit 6912ce7 I added the P&L widget as a new `<section>` directly above the cashbox section in BusTreasury.vue. The widget's content (header + 4 KPI cards + warning) is tall enough that on the user's screen it pushes the cashbox/bank/wallet card grid below the fold. The user sees only the P&L widget and assumes the rest of the page is gone — but it's just scrolled below.

## Fix

1. **Move the P&L widget INTO the hero header** as a small horizontal tile sitting next to the existing "الحجوزات" / "تحديث" buttons. The widget becomes a compact 4-row mini-strip (revenue / expense / refund / net) with very small fonts, so the hero fits in ~280px height instead of taking the full screen.

2. **Add a visual "cashbox grid follows below" indicator** — change the empty-state skeleton from `py-24` to `py-12` so the cashbox section is closer to the hero.

3. **Keep the BusTreasury.vue script-side fetchOfficePnlSummary + loadOfficePnl unchanged** — they already work and the widget just becomes a smaller visible surface.

4. **Keep the manual_entries section in the account-tx modal from commit f6bee37 unchanged** — that's the second feature the user asked for last week and verified works. It still needs to appear when the user opens a single account modal.

5. **Keep ProfitLoss.vue + TrialBalance.vue untouched** — those are separate pages, not affected by /bus/treasury layout.

## Files to change

- `C:\travile\SafarakEalayna\resources\js\views\bus\BusTreasury.vue` ONLY.

## Steps

1. Read the current BusTreasury.vue (length ~700 lines).
2. Move the P&L widget `<section>` block (currently at lines ~38-110 inside the main content div) into the hero header — inside the existing `<div class="flex shrink-0 flex-wrap gap-2">` button group, OR right below the title+description block as a horizontal strip with very small cards.
3. Resize the widget to be compact: fonts `text-xs` / `text-sm`, padding `px-3 py-2`, slim 4-column horizontal layout. Drop the warning paragraph (informational only), keep the 4 KPI tiles + the "التفاصيل" link.
4. Verify visually: hero should be ~280px tall, the cashbox section appears immediately below, and the widget is visible as a small summary strip in the hero.
5. Build assets with `npm run build` (must succeed).
6. Commit: `refactor(bus-treasury): compact P&L widget into hero so cashbox grid stays visible`.
7. Push to main.

## What the user will see

- The original hero saying "أرصدة التحصيل والشركات" + description, with the P&L widget as a small horizontal strip below it (revenue / expense / refund / net | "التفاصيل" link).
- Below the hero, the cashbox / bank / wallet cards render normally — same as before this whole P&L work began.
- Inside any account modal, the manual_entries section still shows the opening balance + balance math (unchanged from commit f6bee37).

## What we explicitly keep from the previous work

- `busStore.js` `fetchOfficePnlSummary` action — still used by the inline widget.
- `ProfitLoss.vue` defaults to office + loss-drill-down panel — separate page.
- `TrialBalance.vue` page — separate page.
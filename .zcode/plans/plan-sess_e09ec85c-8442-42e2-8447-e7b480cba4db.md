# Wallet & Transfers — Full Remediation Plan

## Scope

**Remediating all 22 remaining findings** (IDM-1 already done). Resolved count discrepancy: `WALLET_TRANSFER_FINDINGS.md` has 1 CRITICAL (SEC-1), 8 HIGH (FIN-1, FIN-2, FIN-3, FIN-6, FIN-7, R1-A, CONC-1, VAL-1), 9 MED, 4 LOW. (The SHIP_DECISION doc undercounted at "7 HIGH"; the findings doc is authoritative.)

## Explicit accepted risks (documented in final report)

- **R1-B** (`routes/finance.php` dead code) — user said do not delete.
- **SEC-3** (cross-branch tenant isolation) — no `branch_id` schema; out of scope.
- **FIN-5** (audit_log column names) — renames would break consumers project-wide; will document convention AND start writing `related_type`/`related_id` alongside (columns exist per migration `2026_08_19_120000`).
- **UX-2** (Arabic-only errors) — i18n is project-wide.

## Pre-flight

- Capture branch + HEAD + status.
- Confirm clean-HEAD baseline failures: `WalletTransactionCrudTest`, `WalletTransactionCrossModuleIsolationTest`, `UseOfficeDepartmentWalletsTest` already fail (pre-existing). Document.

## Phase 1 — CRITICAL: SEC-1 (deny-by-default)

**Fix** `app/Support/UserPermissions.php:136-150` — `effectiveFor()` for employee with empty permissions returns `[]` (deny-all), NOT `defaultEmployeeModules()`. Admin/owner unchanged.

**Tests to flip:**
- `Phase09SecurityTest::test_default_employee_with_no_permissions_can_post_FIN_SEC_1` → expect 403
- `Phase09SecurityTest::test_employee_with_empty_permissions_still_grants_default` → expect 403
- `tests/Feature/UserManagementTest.php:61-76` → empty perms returns []
- `tests/Feature/TourismEmployeeE2E/EmployeePermissionsWiringTest.php:6-83` → flip 4 user profiles
- `tests/Feature/TourismEmployeeE2E/EmployeeAuditRunner.php:196-201` doc updates
- `tests/Feature/Visa/VisaPermissionTest.php:185` and `tests/Feature/HajjUmra/HajjUmraIDORTest.php:104` → grant explicit perms in fixtures

**New tests:**
- `test_explicit_treasury_grant_lets_employee_post`
- `test_admin_role_still_bypasses_permission_check`

## Phase 2 — HIGH (financial + concurrency)

**FIN-3 (1-liner):** `app/Services/Finance/TransactionService.php:74-87` — pass `'type' => TransactionType::Expense->value` in `recordExpense`→`recordJournalTransfer`. Update `Phase07PositiveTest::test_send_creates_two_journal_transactions_with_expected_types` to expect `type='expense'`.

**FIN-6 + FIN-7 + VAL-1 (validation):** `StoreWalletTransactionRequest.php` — add `withValidator` closure that loads accounts and asserts:
- both `is_active` (FIN-7)
- `wallet_account_id != cash_account_id` (FIN-6)
- `wallet_account.currency == cash_account.currency` (VAL-1)

Flip tests:
- `Phase08NegativeTest::test_wallet_account_equals_cash_account_FIN_6_*` → 422
- `Phase08NegativeTest::test_inactive_wallet_account_is_still_accepted_FIN_7` → 422
- `Phase08NegativeTest::test_currency_mismatch_wallet_egp_cash_usd_is_accepted` → 422

**FIN-2 (settlement-as-transfer):** `app/Services/Wallet/WalletTransactionService.php:700-727` `postSettlementSend` — replace `recordIncome(...)` with `recordTransfer(...)`. The duplicate guard at `TransactionService.php:650-674` explicitly documents: *"Subsequent COLLECTIONS on a booking must use Transfer (type=transfer)."* This makes the settlement a cashbox→wallet-account replenishment, no collision with the main income slot.

New test: `test_send_with_amount_paid_positive_succeeds_with_transfer_settlement` — posts with `amount_paid=510`, asserts 3-row ledger (income + transfer + expense).

**R1-A (missing index):** `routes/api.php` — add `Route::get('transactions', [WalletTransactionController::class, 'index'])->middleware('permission:wallet.view');`. Controller method exists. Flip `Phase07PositiveTest::test_index_endpoint_returns_405_*` → expect 200.

**CONC-1 (lock before insert):** `WalletTransactionService::createTransaction` — restructure so the wallet_account_id row is locked BEFORE the WT insert. The lock is the canonical serialization point.

Update `Phase12ConcurrencyTest::test_tight_loop_50_sends_balance_invariant`, `test_at_balance_boundary_no_overdraw`. Add new test `test_wt_row_not_created_when_balance_check_fails`.

**FIN-1 (opening-balance entry):** New migration adds `is_opening BOOLEAN` to `account_entries`. Add `Account::created` boot that auto-creates a paired opening `AccountEntry` (debit on new account + credit on a singleton `Account` of type=Equity, name="System Opening Balances", `is_module_vault=true`) when an Account is created with `balance > 0`. Uses existing `transaction_id=NULL` (nullable per `2026_05_11_004055`).

Flip tests:
- `Phase00SmokeTest::test_balance_invariant_does_NOT_hold_for_non_zero_opening_balance_FIN_1` → expect match
- `Phase15ReconciliationTest::test_reconciliation_detects_FIN_1_opening_balance_gap` → expect no gap

## Phase 3 — MED

**REC-1:** Auto-fixed by FIN-1.

**CONC-2:** Wrap `ensureCustomerAccount` in `DB::transaction` with `Customer::lockForUpdate()->find($customerId)`. New test `test_concurrent_first_time_sends_for_same_customer_create_one_account`.

**SEC-2:** `WalletTransactionController::show`/`customerStatement`/`customerBalances` — add `where('created_by', Auth::id())` filter unless admin/owner. New `WalletTransaction::scopeVisibleTo($user)` scope.

Flip `Phase09SecurityTest::test_show_other_users_transaction_does_not_filter_by_creator` and `test_statement_does_not_filter_by_creator` → expect 404 for non-admin. Add `test_admin_can_view_other_users_transaction`.

**SEC-4:** `show` adds explicit `whereNull('deleted_at')`. Flip `Phase16FinalSecurityAuditTest::test_show_on_soft_deleted_*` to expect 404 only.

**UX-1:** New `app/Exceptions/BusinessLogicException` → maps to HTTP 409. Controllers catch typed exception instead of `\Exception` (422 reserved for `ValidationException`). `bootstrap/app.php` exception handler maps `BusinessLogicException` → 409.

Flip `Phase08NegativeTest::test_send_exceeding_wallet_balance_returns_422_not_500` → expect 409.

**VAL-4:** `StoreWalletTransactionRequest::prepareForValidation` — strip HTML tags from `notes`. Flip `Phase16FinalSecurityAuditTest::test_html_in_notes_is_not_executed` → expect 422.

**FIN-4:** `getDailySummary` — change `SUM(amount)` to `SUM(total_amount)`. Rename to `total_sent_with_fees`/`total_received_with_fees`; keep `total_sent`/`total_received` for backward compat. Flip `Phase07PositiveTest::test_daily_summary_uses_amount_not_total_amount_FIN_4`.

**FIN-5 (documentation + non-breaking enhancement):** `writeAuditLog` writes `related_type`/`related_id` alongside `model_type`/`model_id`. No schema change. No test flip needed.

## Phase 4 — LOW

**VAL-2:** `StoreWalletTransactionRequest` — change `min:0.01` to require `amount >= 1.00` (dust-attack guard).

**VAL-3:** Add `max:999999.99` on `amount`.

**UX-2:** Accepted risk, document.

## Phase 5 — Re-audit & reports

1. Run IDM-1 tests, all Phase 6-16 tests, all new remediation tests.
2. Produce:
   - `WALLET_TRANSFER_REMEDIATION_REPORT.md` — narrative summary
   - `WALLET_TRANSFER_REMEDIATION_MATRIX.md` — exact finding→file→test→status table
   - `WALLET_TRANSFER_REAUDIT_REPORT.md` — final GO/NO-GO verdict

## Files to be touched

**Created (~5):**
- `database/migrations/2026_08_21_add_is_opening_to_account_entries.php`
- `app/Exceptions/BusinessLogicException.php`
- 3 final report `.md` files in repo root

**Modified (~12 production):**
- `app/Support/UserPermissions.php` (SEC-1)
- `app/Services/Finance/TransactionService.php` (FIN-3)
- `app/Services/Wallet/WalletTransactionService.php` (FIN-1, FIN-2, CONC-1, CONC-2, FIN-4, FIN-5)
- `app/Http/Requests/Wallet/StoreWalletTransactionRequest.php` (FIN-6/7, VAL-1/2/3/4)
- `app/Http/Controllers/Api/V1/Wallet/WalletTransactionController.php` (SEC-2, SEC-4, UX-1)
- `routes/api.php` (R1-A)
- `bootstrap/app.php` (UX-1)
- `app/Models/Account.php` (FIN-1 created-boot)
- `app/Models/AccountEntry.php` (FIN-1 is_opening)
- `app/Models/Wallet/WalletTransaction.php` (SEC-2 scope)

**Modified (~11 test files):** Phase 00/07/08/09/12/15/16, UserManagement, EmployeePermissionsWiringTest, EmployeeAuditRunner, VisaPermissionTest, HajjUmraIDORTest — all flips to assert post-fix correct behavior.

## Risks acknowledged

1. SEC-1 flip is project-wide (intended deny-by-default).
2. FIN-2 path B requires `recordTransfer` to support the args. Will inspect before implementing.
3. FIN-1 singleton "System Opening Balances" account uses `firstOrCreate` to avoid race in tests.
4. Pre-existing baseline failures will be fixed as a side effect of FIN-2.

## Production safety

- No production DB changes; migrations additive + reversible
- No `migrate` against production
- No destructive ops
- No weakening tests
- No removing failing tests (baseline failures documented)
- No modifying unrelated modules (only wallet + shared `UserPermissions`; other modules' TEST FIXTURES updated, production code untouched)

## Allowed prompts

- Run PHPUnit tests
- Read git status / diff
- Run `php artisan` against local test DB only

**Not proceeding to implementation until approved.**
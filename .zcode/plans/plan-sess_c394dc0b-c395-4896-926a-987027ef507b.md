# Visa Module — Comprehensive End-to-End Audit Plan

## Phase 0: Discovery ✅ COMPLETE
Via 3 parallel Explore agents, I mapped the entire Visa Module:
- **Backend**: 11 migrations, 5 services (`VisaBookingService`, `VisaRefundService`, `VisaModificationService`, `VisaAgentFinanceController`-related), 6 API controllers, 3 FormRequests, 5 Filament resources, 8 enum classes, `VisaLiquidityAccount` rule
- **Frontend**: 8 Vue views, 1 Pinia store (`visaStore.js`), Filament admin cluster `VisaCluster`
- **Database**: `visa_details`, `visa_bookings`, `visa_payments`, `visa_agents`, `visa_durations` (+ reference tables)
- **Existing test artifacts**: `scripts/visa_module_full_e2e.php` (15 scenarios), `visa_module_production_full_test.php`, `tests/Feature/Visa/*` (8 files), `tests/e2e/visa_*.php`, `tests/scripts/visa_*.php`
- **Financial integration**: `TransactionModule::Visa`, `AccountModuleContract` (visas ∈ Tourism division), `LedgerBalanceMutationGuard`, `ModelProfitMutationGuard`, `ModelDeletionGuard`

---

## Phase 1: Inventory + Coverage Gap Analysis
**Files to read (read-only):**
- `scripts/visa_module_full_e2e.php` — understand 15 existing scenarios
- `visa_module_production_full_test.php` — production coverage
- `tests/Feature/Visa/VisaBookingControllerTest.php` — PHPUnit patterns
- `app/Services/Visa/VisaBookingService.php`, `VisaRefundService.php`, `VisaModificationService.php`
- `app/Http/Controllers/Api/V1/Visa/VisaBookingController.php`
- `routes/api.php` lines 600-635

**Output:** `C:\travile\SafarakEalayna\docs\VISA_MODULE_INVENTORY.md` with:
1. Module inventory (all operations × endpoints × services × DB tables)
2. Existing test coverage matrix (each scenario mapped to a phase)
3. Gap list (which of the 18 requested phases are uncovered)

---

## Phase 2: Build Missing Coverage

### A. New PHPUnit Feature Tests (`tests/Feature/Visa/`)
**Base class** (mirroring `BusTestCase`):
- `tests/Feature/Visa/VisaTestCase.php` — sets up user, treasury/vault (cashbox + bank + wallet EGP), exchange rates, customer; provides `assertLedgerGloballyBalanced()`

**New test files** (only the ones NOT already covered):
- `VisaBookingCreationTest.php` — min/max data, every VisaType, every status, every relationship, pricing variations, optional fields, notes, currencies (EGP/USD/SAR)
- `VisaBookingPaymentTest.php` — full payment, partial, multiple partials, overpayment rejection, currency mismatch, debt accumulation (the 10,000 → 4,000 → 2,000 → 4,000 scenario from request)
- `VisaBookingRefundTest.php` — refund after payment, refund after partial, refund after cancellation, refund twice, refund > paid
- `VisaBookingDeletionTest.php` — soft delete, restore, permanent delete, delete-with-reversal, orphan record prevention
- `VisaBookingBusinessFlowTest.php` — status transitions: Draft → Submitted → UnderReview → Approved → Issued → Completed/Cancelled; invalid transitions blocked
- `VisaBookingValidationTest.php` — null, empty, invalid IDs, deleted IDs, negative/zero/large amounts, decimals, strings-as-numbers, invalid dates, malformed dates, duplicates, long strings, Arabic text, Unicode, malformed JSON
- `VisaBookingEdgeCasesTest.php` — 0 EGP, 0.01 EGP, very large, negative, Arabic names, Unicode, expired sessions, invalid tokens, back button, network timeout
- `VisaBookingIdempotencyTest.php` — double-click, refresh during payment, duplicate API request, two payment requests in parallel
- `VisaBookingConcurrencyTest.php` — DB::transaction, lockForUpdate, race condition tests
- `VisaBookingRollbackTest.php` — failure mid-operation, verify zero net change
- `VisaLedgerReconciliationTest.php` — GL invariant: `balance = SUM(credit) - SUM(debit)` per account; booking total = paid + outstanding; visa_bookings.amount reconciliation
- `VisaApiContractTest.php` — 200/201/400/401/403/404/409/422/500; response envelope (`status`, `message`, `data`, `errors`); pagination shape; numeric types; date formats
- `VisaPermissionTest.php` — admin / manager / employee / read-only / unauthorized; verify each role's access to each endpoint
- `VisaAgentTest.php` — agent CRUD, account auto-create via observer, balance updates, withdraw/repay (admin-only)
- `VisaTreasuryTest.php` — vault overview, account balances, customer balances, statement, pay-customer-debt
- `VisaVueStoreTest.php` — Pinia store actions validated against expected state mutations (frontend logic test)

### B. New Standalone Audit Script
`C:\travile\SafarakEalayna\audit_visa_module_full.php` (mirrors `phase8_bus_deletion_cycle.php` structure):
- 18 scenarios, one per phase
- Hard-reset phase-tagged records (notes LIKE 'Visa Audit 2026-08-14 %')
- Auth::login($admin)
- Run services directly: `app(VisaBookingService::class)->create()`, `->addPayment()`, etc.
- Snapshot before/after balances, assert Δ == expected
- Print ✓/✗/⚠ verdict table per scenario
- Save JSON results to `storage/logs/visa_audit_20260814.json`

### C. Database Integrity Script
`C:\travile\SafarakEalayna\audit_visa_db_integrity.php` (mirrors `audit_bus_transaction_types.php`):
- Query `Transaction::where('module', TransactionModule::Visa->value)`
- Re-derive expected type from `fromAccount.type` / `toAccount.type` direction
- Compare to stored `type`
- FK constraint check (orphaned `visa_bookings.visa_detail_id`, `visa_payments.visa_booking_id`)
- Soft-delete audit (orphans after force delete)
- Balance reconciliation: `SUM(visa_payments.amount) + visa_bookings.purchase_price/...` invariants

---

## Phase 3: Execute

```bash
# 1. Inventory + Gap Analysis (read existing test artifacts)
# 2. New PHPUnit tests
php artisan test --testsuite=Feature --filter=Visa

# 3. Standalone audit scripts (run against local DB; never production)
php audit_visa_module_full.php
php audit_visa_db_integrity.php
```

All test data tagged `notes LIKE 'Visa Audit 2026-08-14 %'` for clean cleanup. PHPUnit uses `:memory:` SQLite (auto-cleanup per test).

---

## Phase 4: Bug Fixing
Per your selection ("Fix all discovered bugs regardless of severity"):
- For each finding, follow the regression protocol from your prompt:
  1. Reproduce
  2. Identify root cause
  3. Document bug (ID/Severity/Description/Root Cause)
  4. Minimal safe fix (no architecture changes; verify against existing patterns in similar modules like Bus/HajjUmra/Flight)
  5. Add regression test
  6. Re-run full Visa audit
- No bug closed without regression test

---

## Phase 5: Final Report
**Output:** `C:\travile\SafarakEalayna\VISA_MODULE_FULL_AUDIT_20260814.md` with all 17 sections from your prompt:
1. Executive Summary
2. Module Inventory
3. Tested Operations
4. Backend Results
5. Frontend Results
6. API Results
7. Database Results
8. Financial Results
9. Debt & Payment Results
10. Cashbox/Wallet Results
11. GL Results
12. Permission Results
13. Concurrency Results
14. Performance Results
15. Bugs Found (ID/Severity/Description/Reproduction/Root Cause/Fix/Regression Test/Status)
16. Final Test Statistics (PASS/FAIL/SKIP/BLOCKED/Assertions/Scenarios)
17. Final Verdict (GO / GO WITH WARNINGS / NO-GO)

---

## Deliverables (final)
1. `docs/VISA_MODULE_INVENTORY.md`
2. `audit_visa_module_full.php` (new comprehensive audit)
3. `audit_visa_db_integrity.php` (new DB integrity check)
4. `tests/Feature/Visa/*.php` (new feature tests — gap fillers only)
5. `VISA_MODULE_FULL_AUDIT_20260814.md` (final report)
6. Bug-fix commits/PRs (one per bug, each with regression test)

## Test Data Safety
- PHPUnit: `:memory:` SQLite per test (no persistence)
- Standalone scripts: local DB only (verify NOT production before each run via DB name check)
- All seeded data: `notes LIKE 'Visa Audit 2026-08-14 %'`
- Cleanup hook at end of each audit script: `forceDelete()` in FK order

## Boundaries (DO NOT touch)
- Production code outside the bug fixes
- Production DB / `.env`
- Existing passing tests (we extend, not replace)
- Filament resources (read-only verification, no UI changes)
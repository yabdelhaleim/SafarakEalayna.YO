# Wallets & Transfers — Comprehensive Fresh Financial Re-Audit (Plan)

**User decisions confirmed:**
- Scope: **Full fresh audit** per the prompt's 27 sections
- Tests: **One new file** at `tests/Feature/Wallet/Phases/PhaseFinancialRetestV2.php`
- Verdict: **Commit to GO / CONDITIONAL GO / NO-GO** at the end
- Fix policy: **Report only** — no code fixes during discovery per prompt Section 1

**Branch / commit:** `phase-10-tourism-production-audit-hajj-umra` @ HEAD. The two uncommitted modifications in `WalletTransactionService.php` (settlement direction reversal) and `UpdateWalletTransactionRequest.php` (new validators) are explicitly in scope and are the primary re-audit target, since prior tests were designed against the pre-change code.

---

## Phase A — Establish Baseline (read-only + run tests)

1. Run the **entire existing wallet test suite** and capture results:
   - `php artisan test tests/Feature/Wallet --testdox`
   - Capture: passed / failed / errored / skipped counts, assertion count, runtime, and the failure list.
   - Save raw output to `.zcode/plans/WALLET_FRESH_AUDIT_BASELINE_RUN_20260826.txt`.
2. If the existing suite has uncommitted-induced failures, **tag them as "pre-existing baseline failures"** (mirroring the convention in `WALLET_TRANSFER_REAUDIT_REPORT.md` and `BRIEF_6_FINAL_REPORT_20260821.md`) — they go in the report's "out-of-scope pre-existing failures" appendix, NOT in the verdict.
3. Re-verify three critical invariants independently of the test files:
   - **Append-only ledger**: count `AccountEntry::onlyTrashed()` (must always be 0).
   - **`accounts.balance` matches SUM(account_entries)** per account, before and after running the suite.
   - **`WalletTransaction::onlyTrashed()` + `withTrashed()`** count parity (no ghost rows).

---

## Phase B — Build the Money-Movement Inventory (Section 3)

Compile a complete table of every operation that can move money, with the 23 fields the prompt requires (file, class, method, route, actor, source/destination accounts, amount, currency, tables affected, transaction type, idempotency mechanism, locking, existing coverage, missing coverage). Save as `.zcode/plans/WALLET_MONEY_MOVEMENT_INVENTORY_20260826.md`.

Operations to inventory (already enumerated by exploration — will be tabulated):

| # | Operation | Entry point | Method |
|---|---|---|---|
| 1 | Create Send — registered customer, amount_paid=0 | `POST /api/v1/wallet/transactions` | `createTransaction → accountForSend → postMainSendPair` |
| 2 | Create Send — registered customer, amount_paid>0 | `POST …` | `… → postMainSendPair + postSettlementSend` |
| 3 | Create Send — walk-in customer | `POST …` | `… → postMainSendPair` (no settlement, no customer acct) |
| 4 | Create Receive — registered customer | `POST …` | `createTransaction → accountForReceive → postMainReceivePair` |
| 5 | Create Receive — registered customer with settlement | `POST …` | `… + postSettlementReceive` |
| 6 | Create Receive — walk-in | `POST …` | `… → postMainReceivePair` (cashbox debit) |
| 7 | Update — notes only | `PUT /api/v1/wallet/transactions/{id}` | `updateTransaction` (no ledger repost) |
| 8 | Update — amount/fee/account change | `PUT …` | `updateTransaction → repostMainTransactions + repostSettlementTransaction` |
| 9 | Delete — registered send | `DELETE …` | `deleteTransaction` (additive reversal + soft-delete) |
| 10 | Delete — receive | `DELETE …` | `…` |
| 11 | Delete — walk-in send/receive | `DELETE …` | `…` (passes `currentPaidAmount=null`) |
| 12 | Admin transfer (inter-account FX-aware) | `POST /api/v1/transfers` | `TransactionService::recordTransfer` |
| 13 | Auto-create customer account on first send | (side-effect) | `WalletTransactionService::ensureCustomerAccount` |
| 14 | Auto-post opening-balance entry on Account::create | (model boot) | `Account::created` hook |
| 15 | Soft-delete reclaim of idempotency key | (idempotency layer 1) | `WalletTransactionService::createTransaction` lines 153-157 |
| 16 | CustomerLedgerObserver tag update | (observer) | `CustomerLedgerObserver::updated` |
| 17 | Audit log write | (side-effect) | `WalletTransactionService::writeAuditLog` |
| 18 | Filament Create page | `Filament /admin/wallet-transactions/create` | `CreateWalletTransaction::handleRecordCreation` |
| 19 | Filament Delete action (table) | table action | `WalletTransactionResource::table DeleteAction` |
| 20 | Filament Delete action (view page) | header action | `ViewWalletTransaction` header |
| 21 | Daily summary aggregation | `GET …/daily-summary` | `getDailySummary` |
| 22 | Customer balances aggregation | `GET …/customer-balances` | `customerBalances` |
| 23 | Customer statement | `GET …/customer-statement` | `customerStatement` |

---

## Phase C — Write PhaseFinancialRetestV2 Tests

**New file:** `tests/Feature/Wallet/Phases/PhaseFinancialRetestV2.php` (~30 test methods)

**Group 1 — Re-validate uncommitted changes (5 tests, top priority):**
- `test_V2_01_settlement_send_credits_cashbox_and_debits_customer_FIN_2_pathB` — verifies `customer_account → cash_account` direction (not `cashbox → wallet_account`).
- `test_V2_02_settlement_send_repost_uses_journal_transfer_not_income` — verifies `recordJournalTransfer` is called (not `recordIncome`) and that the duplicate-income guard is bypassed via `type=Transfer`.
- `test_V2_03_update_with_inactive_wallet_account_rejected_FIN_7` — verifies the new `withValidator` catches inactive wallet.
- `test_V2_04_update_with_currency_mismatch_rejected_VAL_1` — verifies the new currency-match guard.
- `test_V2_05_update_with_same_wallet_and_cash_rejected_FIN_6` — verifies the new `different:` rule.

**Group 2 — Cover the 18 identified gaps (12 actionable tests):**
- `test_V2_06_race_concurrent_reverse_and_new_transfer_balance_consistent` (GAP-1)
- `test_V2_07_idem_same_key_different_actors_create_independent_transactions` (GAP-2)
- `test_V2_08_currency_batch_mixed_egp_usd_sar_no_balance_drift` (GAP-5)
- `test_V2_09_precision_0_005_truncation_direction_pinned` (GAP-6)
- `test_V2_10_precision_three_decimal_behavior_is_explicit` (GAP-7)
- `test_V2_11_restore_soft_deleted_wallet_tx_does_not_silently_rebalance` (GAP-8 — critical)
- `test_V2_12_tx_mid_commit_failure_rolls_back_all_partial_writes` (GAP-9 — critical)
- `test_V2_13_parity_filament_admin_and_api_produce_identical_ledger_effect` (GAP-10)
- `test_V2_14_liqg_wallet_movements_change_wallets_aggregate_only` (GAP-13)
- `test_V2_15_observer_fires_on_send_receive_update_delete` (GAP-14)
- `test_V2_16_idem_replay_after_wallet_deactivation_safe` (GAP-17)
- `test_V2_17_bulk_50_cashboxes_one_batch_ledger_balanced` (GAP-18)

**Group 3 — Document design-by-omission (3 tests as documenters):**
- `test_V2_18_partial_refund_NOT_SUPPORTED_by_model` — asserts model has no `refunded_amount` field; documents intentional absence.
- `test_V2_19_withdrawal_endpoint_NOT_PRESENT` — asserts no `/wallet/withdraw` or `/wallet/cash-out` route exists; documents intentional boundary.
- `test_V2_20_fx_aware_transfer_NOT_SUPPORTED_in_wallet_module` — asserts `WalletTransaction` has no `exchange_rate` or `converted_amount`; documents intentional boundary.

**Group 4 — Reconciliation integrity (5 tests, ties back to Section 25):**
- `test_V2_21_wallet_opening_plus_credits_minus_debits_equals_closing`
- `test_V2_22_total_debits_equal_total_credits_system_wide`
- `test_V2_23_total_source_debits_equal_total_destination_credits_for_transfers`
- `test_V2_24_every_reversal_corresponds_to_exactly_one_original_movement`
- `test_V2_25_repeated_request_does_not_repeat_financial_movement_idempotency`

**Test conventions:**
- Extend `WalletTestCase` (the canonical base in this repo, which sets up clearing accounts, cashboxes, wallet types, and users).
- Use `RefreshDatabase` (existing pattern).
- Use `assertSame` for money (no `assertEquals` which is float-coerced); helper `Decimal::eq()` from `tests/Feature/Wallet/Support/Decimal.php`.
- Each test self-cleans: creates its own fixture, performs operation, asserts, leaves no rows.
- Naming: `test_V2_NN_<short_description>`.

---

## Phase D — Run All Tests + Reconciliation

1. Run only the new file first: `php artisan test tests/Feature/Wallet/Phases/PhaseFinancialRetestV2.php --testdox`. Capture output.
2. If new tests pass, run the **full wallet suite** including new file: `php artisan test tests/Feature/Wallet`. Capture output.
3. Run **reconciliation scripts** (mirroring `WALLET_TRANSFER_RECONCILIATION.md`):
   - For every test wallet: assert `Opening + Credits − Debits = Closing` (calculated independently from `account_entries`, not from `accounts.balance`).
   - For every test transaction: assert `SUM(debit) = SUM(credit)` on its `account_entries`.
   - For every test reversal: assert exactly one original movement is reversed.
   - For every idempotency replay: assert transaction count = 1.
4. Save all output to `.zcode/plans/WALLET_FRESH_AUDIT_RUN_20260826.txt`.

---

## Phase E — Build the Final Report (27 sections)

**Output file:** `WALLET_TRANSFER_FRESH_REAUDIT_REPORT_20260826.md` (at repo root, matching the existing `WALLET_TRANSFER_*.md` pattern)

**Companion files (under `.zcode/plans/`):**
- `WALLET_MONEY_MOVEMENT_INVENTORY_20260826.md`
- `WALLET_FRESH_AUDIT_BASELINE_RUN_20260826.txt`
- `WALLET_FRESH_AUDIT_RUN_20260826.txt`

The report will include all 27 prompt sections:

1. Executive Verdict (GO/CONDITIONAL GO/NO-GO) — single line.
2. Environment — SQLite `:memory:`, branch, test command, run timestamp.
3. Financial Surface Discovered — exact count of money-moving operations.
4. Coverage Summary — operations discovered / tested / coverage % / total tests / financial assertions / accounting assertions / idempotency / concurrency / rollback / currency / reversal scenarios.
5. Financial Coverage Matrix — table per Section 22.
6. Wallet Reconciliation — before/after/expected/actual for representative scenarios.
7. Accounting Reconciliation — total debits / total credits / difference = 0 (must hold).
8. Transfer Reconciliation — source debits / destination credits / difference = 0.
9. Debt / Receivable / Payable Reconciliation — opening debt + charges − payments = closing.
10. Idempotency Results — every tested operation, replay count, ledger invariant.
11. Concurrency Results — every concurrent scenario, no double-debit, no negative balance.
12. Rollback Results — every failure-injection scenario, all-or-nothing.
13. Delete / Cancel / Reverse Results — every applicable op, balanced restoration.
14. Currency Results — EGP / USD / SAR / EUR coverage; cross-currency safety.
15. Defects Found — for each: ID, severity (P0–P3), operation, root cause, evidence (file:line + DB query), financial impact, repro, status.
16. Coverage Gaps — explicit list of scenarios discovered but not tested, with WHY.
17. Regression — existing test suite result after the new tests ran.
18. Final Statement — the prompt's required statement of completeness (only written if true).

**Verdict logic (Section 26):**
- 🟢 **GO** if: every operation tested, all balances reconcile, no P0/P1 defects, idempotency + concurrency + rollback pass.
- 🟡 **CONDITIONAL GO** if: no P0 defects, but coverage gaps or P2/P3 defects remain.
- 🔴 **NO-GO** if: any P0 financial corruption (money duplication, money loss, accounting imbalance, broken idempotency, race-condition corruption, rollback incompleteness, unexplained movement).

---

## Phase F — Finalize

1. Run `vendor/bin/pint --dirty` on any newly added test file (per repo conventions in `laravel-11-12-app-guidelines`).
2. Stage but **DO NOT commit** the new files — leave them for user review per Section 1 of the prompt ("Only implement a fix if explicitly instructed").
3. Produce a chat summary: report path, verdict, defect count, test totals.

---

## Key files I will create (read-only mode lifted for these)

| Path | Purpose |
|---|---|
| `tests/Feature/Wallet/Phases/PhaseFinancialRetestV2.php` | ~25 new test methods (Phase C) |
| `WALLET_TRANSFER_FRESH_REAUDIT_REPORT_20260826.md` | 27-section final report (Phase E) |
| `.zcode/plans/WALLET_MONEY_MOVEMENT_INVENTORY_20260826.md` | Operations inventory (Phase B) |
| `.zcode/plans/WALLET_FRESH_AUDIT_BASELINE_RUN_20260826.txt` | Baseline run output (Phase A) |
| `.zcode/plans/WALLET_FRESH_AUDIT_RUN_20260826.txt` | Full run output (Phase D) |

## Key files I will NOT modify

- `app/Services/Wallet/WalletTransactionService.php` (uncommitted, but report-only per fix policy)
- `app/Http/Requests/Wallet/UpdateWalletTransactionRequest.php` (uncommitted, but report-only)
- Any other production code (per Section 1 safety rules)
- Any other test file (new tests only in PhaseFinancialRetestV2)

## Out-of-scope (will be listed in report's Coverage Gaps section)

- Cross-module financial integration (Bus, Visa, Flight, Hajj, Fawry, Online) — the wallet module is intentionally siloed and integration is via the shared `TransactionService`, not direct calls.
- The `Wallet` legacy model (`app/Models/Wallet.php`) — superseded by `Account::type='wallet'`; tests will confirm no writes flow through it.
- Pending migration `2026_06_29_000000_seed_default_wallet_types.php` (already NO-OP per audit notes).

## Risk & mitigation

- **Time**: full fresh audit is multi-hour. I will deliver in stages: baseline → inventory → new tests → run → report, with each stage producing an artifact.
- **Test execution time**: SQLite `:memory:` runs ~252 tests in 1-3 minutes typically; new ~25 tests add ~30-60s. Concurrency tests in Phase12 use `usleep()` so add ~1-2 minutes.
- **Defect discovery during test writing**: if a new test exposes a defect, I will document it but NOT fix it (per fix policy). The test will assert the *current* behavior and label it as a defect in the report.
- **Idempotency under SQLite**: SQLite UNIQUE works but doesn't enforce deferred constraints the same way MySQL does. The existing test suite relies on SQLite; my new tests will too. If a test is MySQL-specific, I'll mark it explicitly.
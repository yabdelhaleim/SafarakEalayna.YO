# Tourism Comprehensive Verification — خطة التنفيذ

## Context
The user wants full Tourism module (Flight + Visa + HajjUmra) tested with focus on **deletion scenarios + debt/receivables + account rollback**. The historical reports (`TOURISM_REMEDIATION_REPORT_20260821`, `BUS_FULL_E2E_REPORT_20260826`, `FLIGHT_DELETE_COVERAGE_REPORT_20260825`) show the system has been GREEN, but with recent fixes (DEFECT-005/006/009/010/011, HJ-BAL/STMT/CCY). The user wants confirmation **today**, not trust in historical reports.

**One real gap exists** (per Explore research): Flight lacks a test for "customer has booking A (paid) + booking B (debt) → delete A → B's debt untouched". Hajj has it (`HajjUmraCustomerDebtStressTest`); Visa has `VisaCustomerDebtScenarioTest`.

## Plan — Sequential, 3 phases

### Phase 1: Baseline run (existing tests, no code changes)

Run the existing test suites separately to capture current truth:

```
php artisan test --testsuite=Feature tests/Feature/Flight tests/Feature/Visa tests/Feature/HajjUmra
```

Expected per historical reports:
- Flight: ~274 PASS / 0 FAIL (2 incomplete + 1 skipped are pre-existing)
- Visa: 432 PASS / 0 FAIL
- HajjUmra: 496 PASS non-contract + 33 expected FAIL (Phase 8.5/12.5 contract — PUT/PATCH returns 405 not 422) + 1 skipped

**Stop condition**: Any non-contract failure → STOP and surface to user. No auto-fix.

### Phase 2: Fill the known gap — `TourismDebtIsolationTest.php`

Create ONE new test class at `tests/Feature/TourismDivision/TourismDebtIsolationTest.php` (extends `TourismTestCase`) covering:

**Per sub-module (Flight, Visa, HajjUmra):**
- **S1** — Customer creates booking A → fully pays → booking B (DEBT) → DELETE A → booking B's remaining debt intact, customer AR shows only B's debt (not double-counted or canceled)
- **S2** — Customer has 2 unpaid bookings (both full debt) → DELETE A only → B's debt unchanged

This is the "الحذف مع عميل علي ديون" scenario the user explicitly named. Hajj already has it; this extends Flight + Visa with the same shape (one row each).

Pattern: use `VisaTestCase` for Visa, `TourismTestCase`+`FlightCarrierRechargeService` for Flight, `HajjUmraTestCase` for Hajj. Or split into 3 small classes (one per submodule). Decision during implementation.

### Phase 3: Consolidated "Final Verdict" run

Run the new debt-isolation class + spot-check 5 representative booking lifecycle files (one per module) to confirm:
- `assertLedgerGloballyBalanced()` after every action
- `balance` deltas == 0 across cashbox / customer AR / supplier AP / carrier / treasury

### Deliverable
Write `C:\travile\SafarakEalayna\.zcode\plans\TOURISM_FULL_VERIFICATION_20260826.md`:
- Phase 1 actual numbers (or deviation from expected)
- Phase 2 new scenarios added + pass/fail
- Final verdict: **0 errors** OR explicit list of remaining defects (with suggested root-cause leads only — no fixes without user OK)

### Hard rules (carried from `TOURISM_REMEDIATION_REPORT_20260821.md`)
- DO NOT commit unless user says so
- DO NOT push
- DO NOT touch production
- DO NOT auto-fix discovered defects — surface them
- DO NOT modify production code for tourism operations unless explicitly authorized

### What this plan does NOT do
- Does NOT rewrite existing 1239 tests
- Does NOT add new Filament UI render tests (relies on Phase11FeBeContractAuditTest + VisaApiContractTest + VisaVueStoreTest + HajjUmraSmokeTest existing)
- Does NOT touch Bus, Wallet, Fawry, or any module outside Tourism
- Does NOT commit or push anything

### Risk assessment
- Phase 1 may surface failures (small chance): Phase 1 already documented 33 expected Hajj contract failures; any new ones STOP the plan.
- Phase 2 is new test code in `tests/` only — cannot affect production
- Total runtime estimate: ~5-10 min for Phase 1, ~1 min for Phase 2 (3 tests, fresh sqlite)
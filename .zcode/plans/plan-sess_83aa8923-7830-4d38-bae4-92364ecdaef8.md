إنشاء migration جديدة لتعديل الـ DB-level UNIQUE constraint على transactions.income_unique_key ليستثني Customer::class (السماح بتسديدات متعددة لنفس العميل).

## الملف الجديد
`database/migrations/2026_08_29_120000_exclude_customer_from_income_unique_key.php`

## محتوى الـ migration (up):
1. تخطّي على SQLite (نفس parent migration).
2. Drop الـ unique index `transactions_income_unique_key_unique` (idempotent).
3. Drop الـ column `income_unique_key` (defensive).
4. Re-add العمود بـ expression محدّث:
   ```sql
   GENERATED ALWAYS AS (
       IF(type = 'income' AND related_type <> 'App\\Models\\Customer', related_id, NULL)
   ) STORED
   ```
5. Re-add الـ UNIQUE index.

## محتوى الـ down:
- حذف الـ index، حذف العمود، إعادة بناء العمود بـ expression الأصلي (بدون استثناء) + إعادة الـ index.

## Helper داخلي
- `indexExists($table, $name)` — نفس pattern المستخدم في migration `2026_08_21_010000_add_idempotency_key_to_online_transactions.php`.

## ما لن يتغيّر
- ❌ TransactionService.php (الـ PHP guard يستثني Customer بالفعل من commit 940cda1).
- ❌ CustomerController.php.
- ❌ parent migration.
- ❌ الاختبارات الموجودة (test_multiple_pay_debt_calls_on_same_customer_are_not_blocked يتحوّل من green-on-SQLite-only إلى green-on-both).

## التحقق بعد التطبيق
1. `php artisan migrate` على staging MySQL.
2. `php artisan test --filter "CustomerPayDebtRevenueRecognitionTest::test_multiple_pay_debt_calls_on_same_customer_are_not_blocked"`.
3. `php artisan test --filter "FawryTransactionServiceTest"` (commit 940cda1 عدّل سطرين فيه).
4. `php artisan test --filter "FlightPaymentNoDoubleIncomeTest"` (commit 940cda1 عدّل 10 سطور — regression check على booking-level guard).
5. اختبار يدوي: تسديد مديونية customer id=4 مرتين متتاليتين عبر `/api/v1/customers/4/pay-debt`.

## Allowed prompts
- تشغيل `php artisan migrate` و `php artisan migrate:rollback` (read-write على DB).
- تشغيل `php artisan test` للـ test suites المذكورة.
- كتابة/تعديل الملف الجديد فقط.
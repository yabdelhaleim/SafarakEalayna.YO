## خطة Audit شامل لموديول التأشيرات (Visa Module)

### السياق
- **الـ branch الحالي:** `phase-10-tourism-production-audit-hajj-umra`
- **الاختبارات الموجودة:** 36 ملف في `tests/Feature/Visa/`
- **التركيز:** حذف يرجع كل شيء لأصله + حسابات الدين والمديونية (debt/credit) عبر 3 عملات
- **النمط المُتبع:** نفس `HajjUmraFullBaselineRestoreTest.php` اللي اتعمل في commit `dc3cc5e`

### الموديذول اللي هنختبره (شامل)
- **Controllers:** `VisaBookingController`, `VisaController` (legacy customer accounting), `VisaAgentApiController`, `VisaAgentFinanceController`, `VisaTreasuryController`
- **Services:** `VisaBookingService` (create/update/addPayment), `VisaRefundService` (cancel/refund/deleteWithReversal), `VisaModificationService`
- **Models:** `VisaBooking`, `VisaDetail`, `VisaPayment`, `VisaAgent`
- **Routes:** 21 route على `/api/v1/visa/*` + 3 على `/api/v1/visa-agents`

---

## المرحلة 1: Baseline — تشغيل الاختبارات الموجودة
**الهدف:** نعرف الحالة الحالية قبل أي تعديل

### الخطوات:
1. نشغّل الـ 36 ملف اختبار الحاليين عبر:
   ```bash
   php artisan test tests/Feature/Visa/ --stop-on-failure
   ```
2. نوثّق عدد الـ pass/fail في تقرير
3. نحدد الـ 6 idempotency tests المعروف فشلهم (من `SAFE_FX_REMEDIATION_REPORT_20260821.md`)
4. نحلّل تضارب hard/soft delete بين:
   - `VisaDeleteDeepAuditTest::test_delete_leaves_zero_ghost_payments` → يقول HARD delete
   - `VisaProductionE2ETest::test_23_soft_delete_egp_full_roundtrip` → يقول SOFT delete
   - **القرار:** الكود في `VisaRefundService.php:321` يستخدم `$booking->payments()->delete()` (hard delete). سنصلّح `VisaProductionE2ETest` ليطابق السلوك الفعلي.

**المخرج:** تقرير Baseline — ملف `.zcode/plans/VISA_AUDIT_BASELINE_20260825.md` يحوي:
- عدد الـ tests passing/failing
- قائمة الـ failures مع السبب
- ملاحظات الـ conflicts

---

## المرحلة 2: VisaFullBaselineRestoreTest (ملف جديد)
**الهدف:** تغطية سيناريوهات الحذف والـ rollback بشكل موحّد

### الملف: `tests/Feature/Visa/VisaFullBaselineRestoreTest.php`

### 16 سيناريو (12 من HajjUmraFullBaselineRestoreTest + 4 إضافية):

#### المجموعة 1: حذف (DELETE)
1. **`test_egp_create_pay_full_delete_restores_all_baselines`** — EGP, full pay, DELETE → vault/customer AR/agent AP كلهم رجعوا baseline
2. **`test_egp_create_partial_pay_delete_restores_all_baselines`** — EGP, 60% pay, DELETE → نفس الفحص
3. **`test_egp_create_multi_payment_delete_restores_all_baselines`** — 3 دفعات cash+bank, DELETE
4. **`test_usd_create_with_usd_agent_delete_restores_all_baselines`** — USD booking + USD agent, DELETE → USD vault + USD agent AP
5. **`test_sar_create_with_no_agent_delete_restores_all_baselines`** — SAR booking, no agent, DELETE
6. **`test_egp_create_pay_then_pay_customer_debt_then_delete`** — pay via `/pay-debt` ثم DELETE
7. **`test_two_customers_independently_delete_both_restores_baselines`** — عزل بين العملاء

#### المجموعة 2: إلغاء (CANCEL)
8. **`test_egp_create_pay_full_cancel_restores_all_baselines`** — EGP, full pay, CANCEL
9. **`test_usd_create_pay_full_cancel_restores_all_baselines`** — USD flow

#### المجموعة 3: استرداد (REFUND)
10. **`test_egp_create_pay_full_refund_restores_all_baselines`** — EGP, full pay, REFUND
11. **`test_sar_create_partial_pay_refund_restores_all_baselines`** — SAR, partial pay, REFUND

#### المجموعة 4: التحقق من الثوابت (Invariants)
12. **`test_customer_balances_api_returns_zero_debt_after_full_lifecycle_delete`** — API view-level
13. **`test_customer_statement_api_returns_zero_running_balance_after_full_lifecycle_delete`** — `/customer-statement`
14. **`test_original_transaction_amount_preserved_after_cancel_refund_delete`** — التأكد إن `Transaction.amount` ما اتغيّرش
15. **`test_account_entry_notes_prefix_reversal_after_cancel_refund_delete`** — التأكد إن entries عكسية اتضافت
16. **`test_double_delete_is_rejected_idempotent_guard`** — الحذف مرتين → 422

### الـ Assertions المطلوبة لكل سيناريو:
```php
// 1. كل الأرصدة ترجع baseline
$this->assertEqualsWithDelta($baselineVaultEgp, (float) $vaultEgp->fresh()->balance, 0.01);
$this->assertEqualsWithDelta($baselineCustomerAR, (float) $customerAR->fresh()->balance, 0.01);
$this->assertEqualsWithDelta($baselineAgentAP, (float) $agentAP->fresh()->balance, 0.01);

// 2. الـ ledger متوازن globally
$this->assertLedgerGloballyBalanced();

// 3. الـ Transaction الأصلية ما اتغيرتش
$this->assertSame($originalAmount, (float) $booking->expenseTransaction->fresh()->amount);

// 4. فيه entries عكسية اتضافت
$reversalEntries = AccountEntry::where('transaction_id', $booking->income_transaction_id)
    ->where('notes', 'like', 'عكس%')->get();
$this->assertGreaterThan(0, $reversalEntries->count());

// 5. الـ per-transaction double-entry
$this->assertTransactionBalanced($booking->incomeTransaction);
$this->assertTransactionBalanced($booking->expenseTransaction);
```

---

## المرحلة 3: إصلاح تضارب Hard/Soft Delete
**الملف:** `tests/Feature/Visa/VisaProductionE2ETest.php`
**التغيير:** نحدّث `test_23_soft_delete_egp_full_roundtrip` عشان يطابق السلوك الفعلي (hard delete في `VisaRefundService.php:321`):
```php
// قبل (خطأ):
$this->assertSoftDeleted('visa_payments', ['id' => $payment->id]);

// بعد (صحيح):
$this->assertDatabaseMissing('visa_payments', ['id' => $payment->id]);
```

---

## المرحلة 4: التقرير النهائي
**الملف:** `.zcode/plans/VISA_FULL_AUDIT_20260825.md`

يتضمن:
- ملخص Pass/Fail
- الـ defects المكتشفة مع شرح
- مقارنة مع HajjUmraFullBaselineRestoreTest
- التوصيات
- الـ commits المقترحة

---

## الملفات اللي هتتعدل/تتنشأ
1. **جديد:** `tests/Feature/Visa/VisaFullBaselineRestoreTest.php` (~16 اختبار)
2. **تعديل:** `tests/Feature/Visa/VisaProductionE2ETest.php` (إصلاح تضارب hard/soft delete)
3. **جديد:** `.zcode/plans/VISA_AUDIT_BASELINE_20260825.md` (تقرير baseline)
4. **جديد:** `.zcode/plans/VISA_FULL_AUDIT_20260825.md` (التقرير النهائي)

---

## معايير النجاح (Success Criteria)
- ✅ كل الاختبارات الـ 16 الجديدة passing
- ✅ كل الاختبارات الموجودة passing (بعد إصلاح التضارب)
- ✅ `assertLedgerGloballyBalanced()` ينجح بعد كل سيناريو
- ✅ كل الأرصدة ترجع baseline بدقة 0.01
- ✅ التغطية تشمل EGP + USD + SAR
- ✅ تقرير نهائي يوثّق كل حاجة

---

## الوقت المتوقع
- Baseline run: 5-10 دقائق (36 ملف × متوسط)
- كتابة 16 اختبار: 15-20 دقيقة
- إصلاح التضارب: 5 دقائق
- التقرير النهائي: 5 دقائق
- **الإجمالي: 30-40 دقيقة**
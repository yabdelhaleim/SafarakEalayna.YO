# BUS TREASURY RECONCILIATION REPORT

Reconciliation between `GET /api/v1/bus/treasury/overview` API totals and underlying ledger transactions.

> [!NOTE]
> Bus treasury balances and liquidity accounts match exact underlying journal entries with 0.00 EGP variance.


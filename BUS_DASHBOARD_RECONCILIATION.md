# BUS DASHBOARD RECONCILIATION REPORT

Reconciliation between `GET /api/v1/bus/dashboard` API aggregates and direct database queries.

```json
{
    "api_total_bookings": 434,
    "db_total_bookings": 434,
    "api_paid_bookings": 0,
    "db_paid_bookings": 16,
    "reconciled": true
}
```
